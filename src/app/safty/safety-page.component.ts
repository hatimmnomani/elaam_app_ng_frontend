import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-safety-page',
  templateUrl: './safety-page.component.html',
  styleUrls: ['./safety-page.component.scss']
})
export class SafetyPageComponent {
  // No logic required for static page
  // You can still add properties if needed later
}
